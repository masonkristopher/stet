export const PROBLEMS_HEIGHT = 10;
/** Rows the references overlay shows before its windowed list scrolls. */
export const REFERENCES_MAX_ROWS = 14;
/** Narrowest a manually-resized sidebar may become. */
export const SIDEBAR_MIN_WIDTH = 24;
/** Columns the viewer is guaranteed, capping how wide the sidebar can grow. */
export const SIDEBAR_VIEWER_MIN = 28;
